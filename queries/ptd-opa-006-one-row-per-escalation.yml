name: "PTD-OPA-006: one row per escalation"
guid: a51e70d2-c560-416e-8372-f34b359368d5
prebuilt: false
platforms:
  - Open Policy Agent
category: A grant split across two decisions
description: >
  Who writes what, through which endpoint, allowed by which decision, and granted by which.
query: |-
  MATCH (a:PTD_Principal)-[e:PTD_CanEscalateTo]->(b:PTD_Principal)
  WHERE 'PTD-OPA-006' IN e.patterns
  RETURN a.name AS principal, b.name AS reaches, e.value AS value, e.via AS via, e.authorized_by AS allowed_by, e.decision AS granted_by
revision: 1
resources:
  - https://github.com/saluc28/petard/blob/main/taxonomy-registry/opa/PTD-OPA-006-write-allowed-by-another-decision.yaml
acknowledgements: []
