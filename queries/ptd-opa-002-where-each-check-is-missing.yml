name: "PTD-OPA-002: where each check is missing"
guid: 523ce455-95d4-4509-aa83-c46693c6c4fe
prebuilt: false
platforms:
  - Open Policy Agent
category: Fail-open on absence
description: >
  The documents each check does not reach, out of how many were tried.
query: |-
  MATCH (r:PTD_Rule)-[e:PTD_Reads]->(a:PTD_Attribute)
  WHERE 'PTD-OPA-002' IN e.patterns
  RETURN r.name AS rule, a.name AS attribute, e.uncovered_keys AS uncovered, e.keys_checked AS checked
  ORDER BY checked DESC
revision: 1
resources:
  - https://github.com/saluc28/petard/blob/main/taxonomy-registry/opa/PTD-OPA-002-deny-undefined-on-missing-data.yaml
acknowledgements: []
